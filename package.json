{
  "name": "atom-updater-linux",
  "main": "./lib/main",
  "version": "0.1.0",
  "author": "Andy Richardson (forked from Rodrigo Pinheiro Matias atom-updater-notify)",
  "description": "Used to check for updates to Atom and download and install them in linux.",
  "keywords": [
    "update",
    "atom",
    "updater",
    "linux",
    "fedora",
    "download"
  ],
  "consumedServices": {
    "status-bar": {
      "versions": {
        "^1.0.0": "consumeStatusBar"
      }
    }
  },
  "configSchema": {
    "acceptTestVersion": {
      "type": "boolean",
      "default": false,
      "title": "Accept test versions of atom.",
      "description": "You can accept test versions of atom."
    },
    "checkInterval": {
      "type": "integer",
      "default": 1,
      "title": "Check interval",
      "description": "Interval to check new versions of atom in repository, in hours."
    },
    "pkgType": {
      "type": "string",
      "default": "Debian, Ubuntu",
      "title": "Package type",
      "enum": [
        "Debian, Ubuntu",
        "RedHat",
        "Mac OSX"
      ],
      "description": "The type of package for download."
    }
  },
  "repository": "https://github.com/andyrichardson/atom-updater-linux.git",
  "license": "MIT",
  "engines": {
    "atom": ">=1.0.0 <2.0.0"
  },
  "dependencies": {
    "dom-listener": "^0.1.2",
    "request": "^2.81.0",
    "shelljs": "^0.7.7"
  }
}
